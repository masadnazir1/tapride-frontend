services:
  tapride-frontend:
    container_name: tapride-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8070:8070"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
